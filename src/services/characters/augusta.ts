import { createPlainBasicCharacter } from "@/services/characters/basic";

export const augustaCharacter = createPlainBasicCharacter({
  id: "augusta",
  displayName: "Augusta",
  attribute: "Electro",
});
