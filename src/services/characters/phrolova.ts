import { createPlainBasicCharacter } from "@/services/characters/basic";

export const phrolovaCharacter = createPlainBasicCharacter({
  id: "phrolova",
  displayName: "Phrolova",
  attribute: "Havoc",
});
